## What this changes

<!-- The behaviour that is different afterwards, not the files touched. -->

## Why

<!-- The constraint or the bug. If it argues with a line in README's
     "Decisions", say so here. -->

## Checks

- [ ] `./scripts/check-all.sh` passes (`skipped` lines noted below, if any)
- [ ] If a primitive, prop or style name was added: every duplicated list was
      updated and its check script passes
- [ ] If a host was touched: the change compiles for every target it affects,
      not only the one it was written on

## Platforms exercised

<!-- Which of iOS / Android / macOS / tvOS / visionOS / watchOS / Wear OS this
     was actually run on, and which are untested. -->
